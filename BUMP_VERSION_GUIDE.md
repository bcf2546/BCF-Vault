# วิธี Release เวอร์ชันใหม่

ทุกครั้งที่แก้ HTML แล้วอยากให้ **ผู้ใช้เห็นแบนเนอร์ "มีเวอร์ชันใหม่"** อัตโนมัติ:

## ขั้นตอน (ทำ 3 ที่ให้ตรงกัน)

### 1. แก้ใน `index.html` — บรรทัดต้น script
ค้นหา:
```js
const BCF_VERSION = "18.17";
const BCF_BUILD = "20260507-0056";
```

เปลี่ยนเป็นเวอร์ชันใหม่ เช่น:
```js
const BCF_VERSION = "18.18";
const BCF_BUILD = "20260510-1430";
```

### 2. แก้ใน `index.html` — version label (header)
ค้นหา:
```html
<div id="bcfVersionLabel" ... >v18.17 · build 20260507-0056</div>
```
เปลี่ยนตัวเลขให้ตรงกัน (จะถูก override ด้วย JS อยู่แล้ว แต่เปลี่ยนเพื่อให้ตอนแรกที่ render เห็นค่าถูกต้อง)

### 3. แก้ `version.json`
```json
{
  "version": "18.18",
  "build": "20260510-1430",
  "buildTime": "2026-05-10T14:30:00+07:00",
  "notes": "อะไรที่แก้"
}
```

### 4. Commit ทั้ง 2 ไฟล์ขึ้น GitHub พร้อมกัน
- `index.html`
- `version.json`

## ที่เกิดขึ้นกับผู้ใช้

- ภายใน 5 นาที ผู้ใช้ที่เปิดเว็บค้างจะเห็นแบนเนอร์ "🎉 มีเวอร์ชันใหม่ v18.18 — กดเพื่ออัพเดต"
- กด "รีโหลดเลย" → ได้เวอร์ชันใหม่
- หรือผู้ใช้สลับ tab กลับมา → เช็คอัตโนมัติ
- หรือคลิกที่ "v18.17 · build ..." ตรงหัวเว็บเพื่อ force check ทันที

## เคล็ดลับ build number
ใช้รูปแบบ `YYYYMMDD-HHMM` (เวลาไทย) จะได้ไม่ต้องคิดเลข
เช่น 10 พ.ค. 2026 14:30 → `20260510-1430`
